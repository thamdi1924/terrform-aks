# Azure DevOps pipeline for Azure deployment
- task: TerraformTaskV3@3
  inputs:
    provider: 'azurerm'
    command: 'plan'

#variables:
#- group: terraform-tuesdays

trigger:
  branches:
    include:
    - main
  # paths:
  #  include:
  # - 2021-05-11-ADO/vnet

stages:
- stage: Validate
  displayName: Validate
  jobs:
  - job: validate
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: TerraformInstaller@0
      displayName: Install Terraform
      inputs:
        terraformVersion: 'latest'

  # Init
    - task: TerraformTaskV3@3
      inputs:
        provider: 'azurerm'
        command: 'init'
        backendServiceArm: 'sc-terraformpuffersoft'
        backendAzureRmResourceGroupName: 'rg-tarig'
        backendAzureRmStorageAccountName: 'stterraformpuffersoft'
        backendAzureRmContainerName: 'stterraformstate'
        backendAzureRmKey: 'terraform.tfstate'

    - task: TerraformTaskV3@3
      inputs:
        provider: 'azurerm'
        command: 'plan'



